"use client";

import { CredentialsErrorBoundaryFallback } from "../credentials/credentials-error-boundary";

// Reuse credentials error boundary for now
export const SkillsErrorBoundaryFallback = CredentialsErrorBoundaryFallback;
